# Private / personal notes (never commit)
JS_Context.md
Trading_Bot_Context.md
ARCHITECTURE.md
# BUILD_SPEC.md contains Dad's strategy IP — keep private for now
BUILD_SPEC.md

# Reference material / large binaries (keep out of git)
*.pdf

.env
alertengine/rules/_private/
alertengine/settings_local.py
*.log

# Pre-screen input (Dad's curated watchlist) and nightly output
alertengine/data/
*.csv
__pycache__/
.venv/

# Python build/editable-install metadata
*.egg-info/
build/
dist/

# Terraform — never commit state (may contain secrets) or real var values
infra/terraform/.terraform/
infra/terraform/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
*.tfplan
crash.log
crash.*.log
# Lambda build artifacts
infra/lambda/**/*.zip

# Terraform Lambda build artifacts
infra/terraform/.build/
