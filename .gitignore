# Private / personal notes (never commit)
JS_Context.md
Trading_Bot_Context.md
# BUILD_SPEC.md contains Dad's strategy IP — keep private for now
BUILD_SPEC.md

# Reference material / large binaries (keep out of git)
*.pdf

.env
alertengine/rules/_private/
alertengine/settings_local.py
*.log
__pycache__/
.venv/

# Python build/editable-install metadata
*.egg-info/
build/
dist/
